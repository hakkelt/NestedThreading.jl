var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Loop-macros","page":"API reference","text":"","title":"Loop macros"},{"category":"section","location":"api/#Scoped-budgets","page":"API reference","text":"","title":"Scoped budgets"},{"category":"section","location":"api/#Registry","page":"API reference","text":"","title":"Registry"},{"category":"module","location":"api/#NestedThreading","page":"API reference","text":"NestedThreading\n\nCoordinate a shared CPU thread budget across independently-threaded libraries (BLAS, FFTW, Polyester, NFFT, …) so that an outer parallel loop does not oversubscribe the machine with inner threading — Julia's answer to Python's threadpoolctl.\n\nThe entry points are @budgeted_threads / @budgeted_batch for loops, @budgeted for a loop construct you want to keep exactly as written, and with_restricted_threads / with_full_threads for call sites that are not loops. Libraries register themselves through package extensions; see register_counted_pool! to add one this package does not ship.\n\n\n\n\n\n","title":"NestedThreading"},{"category":"macro","location":"api/#NestedThreading.@budgeted","page":"API reference","text":"@budgeted <parallel-loop>\n\nWrap an already-chosen parallel loop so that its body runs with inner threaded libraries limited to budget_for(range) threads. The loop construct is emitted unchanged — this macro never decides whether to parallelize, only what budget the workers get.\n\n@budgeted Threads.@threads for j in 1:num_threads\n    mul!(view(out, :, j), op, view(inp, :, j))\nend\n\nAccepts any chain of macros ending in a single for x in range loop, so Threads.@threads :static for, @inbounds Polyester.@batch for, and @batch minbatch=64 for all work. The range expression is hoisted into a temporary and evaluated exactly once. A chain containing @batch automatically excludes the :polyester pool, since there the loop being budgeted is itself the Polyester consumer.\n\nSee also @budgeted_threads, @budgeted_batch.\n\n\n\n\n\n","title":"NestedThreading.@budgeted"},{"category":"macro","location":"api/#NestedThreading.@budgeted_threads","page":"API reference","text":"@budgeted_threads [threads = <bool>] for x in range ... end\n\nThreads.@threads over range with an automatic inner thread budget, plus a runtime switch for whether to parallelize at all.\n\nthreads = true (the default when omitted) runs Threads.@threads with the body's inner libraries limited to budget_for(range).\nthreads = false runs a plain sequential loop with inner libraries limited to a single thread, on the assumption that a caller who switched threading off did so because concurrency is happening somewhere else.\n\nBody-level macros such as @inbounds may wrap the loop; they are rebuilt around the emitted Threads.@threads. A macro that parallelizes the loop itself (Threads.@threads, Polyester.@batch, @distributed, …) is rejected at expansion, since this macro emits one already — use @budgeted to budget a parallel loop you have written yourself. @budgeted is also the way to combine this with a macro that insists on being handed a bare for loop, such as @simd.\n\n\n\n\n\n","title":"NestedThreading.@budgeted_threads"},{"category":"macro","location":"api/#NestedThreading.@budgeted_batch","page":"API reference","text":"@budgeted_batch [threads = <bool>] for x in range ... end\n\nPolyester @batch over range with an automatic inner thread budget and the same threads = switch as @budgeted_threads.\n\nThe calling module must have Polyester available (import Polyester), since the expansion emits Polyester.@batch. The :polyester pool is excluded from the budget scope so that the generated @batch loop is not disabled by its own restriction.\n\nAs with @budgeted_threads, a chain that already parallelizes the loop is rejected at expansion; pass @batch's own options to @budgeted instead (@budgeted Polyester.@batch minbatch=64 for ...).\n\n\n\n\n\n","title":"NestedThreading.@budgeted_batch"},{"category":"function","location":"api/#NestedThreading.with_restricted_threads","page":"API reference","text":"with_restricted_threads(f; exclude = ())\n\nRun f() with every registered threaded library limited to a single thread. Equivalent to with_thread_budget(f, 1). Use at call sites that are not a plain for loop — a @sync/@spawn block, or a single call gated on a Bool field.\n\n\n\n\n\n","title":"NestedThreading.with_restricted_threads"},{"category":"function","location":"api/#NestedThreading.with_full_threads","page":"API reference","text":"with_full_threads(f; exclude = ())\n\nRun f() requesting full threading: every CountedPool at capacity and every GuardedPool enabled. Unlike a plain unwrapped call this actively turns guarded libraries on (e.g. sets NFFT._use_threads[] = true), which is what a call site with an explicit threaded = true switch needs.\n\nStill clamped by any outer restriction, so calling it inside a saturated batch loop is safe and does nothing.\n\n\n\n\n\n","title":"NestedThreading.with_full_threads"},{"category":"function","location":"api/#NestedThreading.enable_full_threading","page":"API reference","text":"enable_full_threading()\n\nPermanently set every registered CountedPool to its registration-time maximum — the process-wide escape hatch for code that only ever runs one thing at a time and wants every library at full throttle.\n\nThis sets the baseline: the value restored when the last budget scope exits. Scoped restrictions still win while they are active; this only changes what the process returns to when nothing is restricted.\n\nNot scoped and not undoable — there is deliberately no disable_full_threading.\n\n\n\n\n\n","title":"NestedThreading.enable_full_threading"},{"category":"function","location":"api/#NestedThreading.with_thread_budget","page":"API reference","text":"with_thread_budget(f, n::Integer; exclude = ())\n\nRun f() with every registered threaded library limited to n threads, restoring the previous state afterwards (also on exception). Returns f()'s value.\n\nThe budget actually applied is minimum over all budget scopes active in the process, so\n\nnesting can only ever narrow, never widen, an outer restriction — an inner with_thread_budget(f, 8) inside an outer with_thread_budget(g, 1) still runs at 1;\nconcurrent scopes cannot corrupt each other's bookkeeping: the counts are snapshotted on the first entry and restored on the last exit.\n\nGuardedPools are invoked only when the applied budget is below capacity, and receive that budget. Polyester's guard switches it off outright rather than limiting it proportionally — a partial limit was tried and measured far worse in the saturated regime; see GuardedPool. Pass pool names in exclude to skip guarding a specific pool; @budgeted_batch uses exclude = (:polyester,) because there the outer loop is the Polyester consumer.\n\nnote: Guards are decided once, at entry\nCounted pools follow the applied minimum for as long as the scope is open — another task narrowing the budget lowers them immediately. Guards cannot work that way: a guard is a lexical wrapper around f(), so whether it runs is decided from the budget at entry and cannot be revised afterwards.So a scope that enters while nothing is restricted runs its body unguarded even if a concurrent task restricts the process meanwhile: its BLAS and FFTW counts drop, but a nested Polyester loop inside it can still spawn workers. The other direction is safe — a scope that entered restricted stays guarded for its whole body even after the other scope exits. Only the guarded libraries are affected, and only for scopes that opened unrestricted; if it matters, open the restricting scope before spawning the tasks rather than from inside them.\n\nnote: A budget of `capacity()` raises, it does not merely permit\nThe budget is applied to every counted pool unconditionally, so a scope whose budget works out to capacity — with_full_threads, or a loop with a single item — sets the pools up to that value for its duration, even past a lower count the caller had configured (BLAS.set_num_threads(2), say). The original counts are restored on exit like any other scope. This is what makes with_full_threads able to turn NFFT on; if a hand-tuned lower count must be preserved, do not open a full-throttle scope around it.\n\nnote: Process-global state\nBLAS and FFTW thread counts are process-global with no per-task scoping. Taking the minimum over all active scopes is deliberately conservative: while any task is restricted, unrelated concurrent tasks are restricted too. That never oversubscribes, but it does mean a long-lived restricted scope throttles the whole process. This is a limitation of the underlying C libraries, not something this package can solve.\n\nSee also with_restricted_threads, with_full_threads.\n\n\n\n\n\n","title":"NestedThreading.with_thread_budget"},{"category":"function","location":"api/#NestedThreading.register_counted_pool!","page":"API reference","text":"register_counted_pool!(get, set; name::Symbol)\n\nRegister a library controlled by a thread-count getter/setter pair. Called from NestedThreading's own package extensions, and available to any downstream package that wants to add a library this package does not ship an extension for:\n\nNestedThreading.register_counted_pool!(MyLib.get_threads, MyLib.set_threads; name = :mylib)\n\nRegistering a name that is already present is a no-op, so duplicate extensions across packages are harmless.\n\nRegistration is expected to happen at load time (__init__), before any concurrent use. Registering while a budget scope is already open is nevertheless handled: the newcomer's current count becomes its restore value and the restriction in force is applied to it immediately, so it is restored along with everything else when the last scope exits.\n\n\n\n\n\n","title":"NestedThreading.register_counted_pool!"},{"category":"function","location":"api/#NestedThreading.register_guarded_pool!","page":"API reference","text":"register_guarded_pool!(guard; name::Symbol)\n\nRegister a library that only has a scoped on/off switch. guard is called as guard(f, budget::Int) and must run f() with that library limited to budget threads, returning f()'s value; see GuardedPool for the full contract.\n\nRegistering a name that is already present is a no-op, so duplicate extensions across packages are harmless. Registration is expected to happen at load time (__init__); a guard registered while a budget scope is already open is not retroactively applied to it.\n\n\n\n\n\n","title":"NestedThreading.register_guarded_pool!"},{"category":"type","location":"api/#NestedThreading.CountedPool","page":"API reference","text":"CountedPool(name::Symbol, get, set)\n\nA threaded library that exposes a persistent thread count through a getter/setter pair, e.g. BLAS.get_num_threads/BLAS.set_num_threads or FFTW.get_num_threads/ FFTW.set_num_threads.\n\nRegister with register_counted_pool!.\n\n\n\n\n\n","title":"NestedThreading.CountedPool"},{"category":"type","location":"api/#NestedThreading.GuardedPool","page":"API reference","text":"GuardedPool(name::Symbol, guard)\n\nA threaded library whose only control is a scoped context manager, e.g. Polyester's disable_polyester_threads.\n\nguard must be callable as guard(f::Function, budget::Int) and must run f() with that library limited to budget threads, returning f()'s value. It is only ever called with budget < capacity(); an unrestricted scope skips guards entirely.\n\nMost such libraries can only be switched fully off, which is the right default: a partial limit was implemented for Polyester (which supports one) and measured 320x worse in the saturated regime, because at high thread counts a nested parallel loop is catastrophic however few workers it gets. See the Polyester extension for the numbers. budget is passed anyway so a library with a genuinely proportional control can use it.\n\nUnlike CountedPool this field cannot be a FunctionWrapper: the guard receives an arbitrary caller closure, whose type is not known until the call site, so one runtime dispatch per guarded scope is inherent. It happens once per budget scope, not once per loop iteration.\n\nPrefer CountedPool whenever the library exposes an imperative setter, even a boolean one — a counted pool goes through this package's refcounted snapshot/restore and is therefore safe under concurrency, whereas a guard that saves and restores a global itself reintroduces the very interleaving bug this package exists to fix. A boolean toggle maps cleanly onto a count; see the NFFT extension for the pattern. GuardedPool is for APIs like Polyester's, which are reservation-based and have no imperative form.\n\nRegister with register_guarded_pool!.\n\n\n\n\n\n","title":"NestedThreading.GuardedPool"},{"category":"function","location":"api/#NestedThreading.capacity","page":"API reference","text":"capacity() -> Int\n\nThe number of worker threads a parallel loop is assumed to be able to occupy. Used as the numerator of the automatic budget arithmetic and as the \"unrestricted\" reference value.\n\nDefined as Threads.threadpoolsize(). This is exact for Threads.@threads and an approximation for Polyester's @batch; it is a single function so that assumption lives in one place.\n\n\n\n\n\n","title":"NestedThreading.capacity"},{"category":"function","location":"api/#NestedThreading.budget_for","page":"API reference","text":"budget_for(range) -> Int\n\nThe per-worker inner thread budget for a parallel loop over range: max(1, capacity() ÷ length(range)).\n\nWith capacity() == 8, a loop of 32 items gets budget 1 (the outer loop already saturates the machine) while a loop of 2 items gets budget 4 (2 workers × 4 inner threads). Iterators of unknown length fall back to the conservative budget of 1.\n\n\n\n\n\n","title":"NestedThreading.budget_for"},{"category":"section","location":"extending/#Adding-a-library","page":"Adding a library","text":"A library is registered as one of two kinds of pool.","title":"Adding a library"},{"category":"section","location":"extending/#Counted-pools","page":"Adding a library","text":"Anything with an imperative thread-count setter:\n\nfunction __init__()\n    NestedThreading.register_counted_pool!(\n        MyLib.get_threads, MyLib.set_threads; name = :mylib\n    )\nend\n\nThe pool then participates in the refcounted snapshot/restore, so it is safe under concurrency for free.\n\nA boolean toggle is a counted pool too. Map it onto a count rather than reaching for a guard — this is how the NFFT extension is written:\n\n_full() = max(2, NestedThreading.capacity())\n_get_threads() = MyLib.threading_enabled[] ? _full() : 1\n_set_threads(n) = (MyLib.threading_enabled[] = n >= _full())\n\nNestedThreading.register_counted_pool!(_get_threads, _set_threads; name = :mylib)\n\nThe threshold is capacity() rather than 1 because a library with no partial control should be off whenever anything is restricted; see Composition rules.\n\nwarning: Clamp the threshold to 2\nThe getter and setter must round-trip: set(get()) has to be a no-op, because that is exactly what the last scope exit does. With a bare capacity() threshold that breaks in a single-threaded session, where capacity() == 1 makes \"restricted to 1\" and \"full throttle\" the same number — the getter maps false to 1 and the setter maps 1 back to true, so every budget scope silently enables a library the user had switched off and never restores it. max(2, capacity()) is identical everywhere else and costs only that with_full_threads will not enable the library on a machine with one thread, where its threaded path has no workers anyway.","title":"Counted pools"},{"category":"section","location":"extending/#Guarded-pools","page":"Adding a library","text":"Only for libraries whose sole control is a scoped context manager with no imperative form — Polyester's disable_polyester_threads is the motivating case:\n\n# `guard` is only ever called when a restriction is in force; `budget` is the applied\n# thread budget, provided in case the library has a proportional control.\n_guard(f, budget::Int) = MyLib.without_threads(f)\n\nNestedThreading.register_guarded_pool!(_guard; name = :mylib)\n\nSwitching the library fully off is usually right even when a partial limit is available; see Composition rules for the measurements behind that.\n\nwarning: Do not write a guard that saves and restores a global\nA guard of the form prev = X[]; X[] = new; try f() finally X[] = prev end reintroduces exactly the interleaving bug described in Composition rules — two concurrent guards will capture each other's temporary value. If the library has an imperative setter, register it as a counted pool instead and let the registry do the bookkeeping. Polyester is safe because disable_polyester_threads reserves worker threads rather than saving and restoring a count.","title":"Guarded pools"},{"category":"section","location":"extending/#Doing-it-from-an-extension","page":"Adding a library","text":"Registration belongs in __init__ so that it happens exactly once, when both your package and the library are loaded. If you own neither package, a small extension in your own package works the same way:\n\n[weakdeps]\nMyLib = \"...\"\n\n[extensions]\nMyPkgMyLibExt = \"MyLib\"\n\nmodule MyPkgMyLibExt\nimport MyLib, NestedThreading\n__init__() = NestedThreading.register_counted_pool!(\n    MyLib.get_threads, MyLib.set_threads; name = :mylib\n)\nend\n\nRegistering the same name twice is a no-op, so duplicate extensions across packages are harmless.","title":"Doing it from an extension"},{"category":"section","location":"composition/#Composition-rules","page":"Composition rules","text":"Everything in this package reduces to NestedThreading.with_thread_budget. The rules below are what make it safe to use from library code that does not know what its caller is doing.","title":"Composition rules"},{"category":"section","location":"composition/#Budgets-are-refcounted,-process-wide","page":"Composition rules","text":"The registry keeps a multiset of every budget scope currently active anywhere in the process.\n\nOn the transition from no scope to one scope, the current thread counts are snapshotted.\nWhile any scope is open, the applied count is minimum over all active budgets.\nOn the transition back to no scope, the snapshot is restored.","title":"Budgets are refcounted, process-wide"},{"category":"section","location":"composition/#Nesting-only-ever-narrows","page":"Composition rules","text":"with_thread_budget(1) do\n    with_thread_budget(8) do\n        BLAS.get_num_threads()   # == 1\n    end\nend\n\nAn inner scope asking for more threads cannot widen a restriction an outer caller is relying on, because the applied value is the minimum. This matters for operator algebra: a batched operator whose body contains another batched operator, or an NFFT operator constructed with threaded = true that ends up being called from inside a saturated loop, both compose correctly without either site knowing about the other.","title":"Nesting only ever narrows"},{"category":"section","location":"composition/#Concurrent-scopes-cannot-corrupt-each-other","page":"Composition rules","text":"This is the failure mode the refcounting exists to prevent. With per-scope save/restore — even with a lock around each half — two tasks interleave:\n\n task A task B BLAS\n1 saves 8, applies 1  1\n2  saves 1, applies 1 1\n3 restores 8  8\n4  restores 1 1\n\nand the process is left permanently throttled to 1 with no scope active. A mutex does not fix this: it is the pairing of save and restore that interleaves, not the individual mutations. Snapshotting once and restoring once removes the failure entirely.","title":"Concurrent scopes cannot corrupt each other"},{"category":"section","location":"composition/#The-cost:-conservatism-across-unrelated-tasks","page":"Composition rules","text":"BLAS and FFTW thread counts are process-global; the C libraries have no per-task scoping and nothing in Julia can give them any. Taking the minimum over all active scopes therefore means:\n\nwarning: While any task is restricted, unrelated concurrent tasks are restricted too.\nA long-lived restricted scope throttles the whole process for as long as it is open. This is deliberate — the alternative direction (letting the widest request win) oversubscribes, which is the problem this package exists to solve — but it is worth knowing before opening a budget scope around a long-running operation.","title":"The cost: conservatism across unrelated tasks"},{"category":"section","location":"composition/#All-or-nothing-libraries","page":"Composition rules","text":"Polyester and NFFT are switched off whenever the applied budget is below NestedThreading.capacity, not only when it is 1: a budget of 4 inside an 8-thread outer loop still means the machine is fully occupied, and a nested Polyester loop spawning its own workers on top of that is exactly the oversubscription being avoided. NFFT has no partial control at all; Polyester does, and it is deliberately not used — see the measurements below.\n\nThe one exception is a loop that is the Polyester consumer. @budgeted_batch passes exclude = (:polyester,) so that the @batch loop being budgeted is not disabled by its own restriction.\n\nwarning: Measure in the regime you deploy in\nWhether budgeting helps depends almost entirely on how Threads.nthreads() compares to the machine's core count, and the difference is not a small factor. Both benchmarks from test/runtests.jl :benchmark, run on the same 48-core machine at two thread counts:benchmark nthreads() unbudgeted budgeted \nnt×GEMM(384) 8 0.0280 s 0.0190 s 1.5x\nnt×GEMM(384) 48 85.998 s 0.102 s 846x\nnt×(@batch over 16384) 8 0.00256 s 0.00405 s 0.63x — loses\nnt×(@batch over 16384) 48 1.103 s 0.0135 s 82xAt nthreads() well below the core count there is spare hardware, nesting is free, and restricting only wastes threads — the Polyester row actually comes out negative there. At nthreads() near the core count the unbudgeted versions collapse: 86 seconds for work that takes a tenth of a second. Benchmarks run in the first regime will tell you this package is useless, and that conclusion does not transfer.BenchHelpers.regime_warning in the test suite flags the mismatch when it sees one.\n\nnote: Why guarded pools are all-or-nothing\nPolyesterWeave can reserve some worker slots, so an obvious refinement is to reserve only the capacity() ÷ budget the outer loop is using and leave the rest for nested loops. That was implemented and measured: 2.7x better at nthreads() = 8 with 2 outer iterations, and 320x worse at nthreads() = 48 with 2 outer iterations. At high thread counts a nested parallel loop is catastrophic however few workers it gets, so leaving any free reopens exactly the pathology being closed. All-or-nothing costs a bounded factor in the under-subscribed regime and avoids an unbounded one in the saturated regime, so that is what the Polyester guard does.\n\nnote: Budget scopes themselves are free\nThe benchmark suite asserts this rather than claiming it. Against a plain Threads.@threads loop, three controls all come out at 0.99-1.00x: a do-nothing closure wrapper, a budget scope with the :polyester pool excluded, and the Polyester guard wrapped around a loop with no @batch in it at all. Whatever budgeting costs or saves, none of it is overhead from this package.Numbers from julia --project=test test/runtests.jl :benchmark; rerun them on your own machine and thread count. The benchmarks use BenchmarkTools with paired measurements — baseline and variant back-to-back within each round, median of the per-round ratios — because on a dual-socket machine the same loop is bimodal depending on how threads land across sockets, by far more than the effects being measured. Paired, the control numbers reproduce to within 1% run to run.","title":"All-or-nothing libraries"},{"category":"section","location":"composition/#Guarded-pools-are-decided-at-entry,-counted-pools-are-not","page":"Composition rules","text":"The two kinds of pool track the process-wide minimum differently, and the difference is observable.\n\nA counted pool is a global that the registry writes on every entry and exit, so it always reflects the current minimum: if another task opens a narrower scope while yours is running, your BLAS and FFTW counts drop with it. A NestedThreading.GuardedPool has no global to write — the guard is a lexical wrapper around f(), and f() is already running. Whether a guard was entered is therefore decided once, from the budget at entry, and cannot be revised.\n\n task A task B BLAS (counted) Polyester (guarded)\n1 opens a full-capacity scope  capacity() enabled\n2  opens a scope at 1 1 disabled for B only\n3 body runs body runs 1 A's nested @batch still spawns workers\n\nSo a scope that opens while nothing is restricted runs its body unguarded even if the process is restricted underneath it a moment later. Note what is not affected: the counted pools are correct throughout (row 3, BLAS is at 1 for both tasks), and the opposite direction is safe — a scope that entered restricted stays guarded for its whole body even after the other scope exits, which is conservative rather than oversubscribing.\n\nThe window only exists for a scope that opened unrestricted, and only for guarded libraries — today that is Polyester alone. It is not closable: disable_polyester_threads reserves worker slots around a call, and there is no way for another task to enter that reservation around a call already in flight. If it matters for your workload, open the restricting scope before spawning the tasks rather than from inside them, which is what the loop macros do — they wrap the loop, not the loop body.","title":"Guarded pools are decided at entry, counted pools are not"},{"category":"section","location":"composition/#A-full-budget-raises,-it-does-not-merely-permit","page":"Composition rules","text":"The applied budget is written to every counted pool unconditionally, so a scope that works out to NestedThreading.capacity — with_full_threads, or a loop with a single item — sets the pools up to that value, past a lower count the caller may have configured by hand:\n\nBLAS.set_num_threads(2)\n@budgeted_threads for i in 1:1        # budget == capacity()\n    BLAS.get_num_threads()            # == capacity(), not 2\nend\nBLAS.get_num_threads()                # == 2 again\n\nThis is deliberate: it is what lets with_full_threads turn a default-off library such as NFFT on. The snapshot/restore applies as usual, so the hand-set value comes back when the scope closes — but a hand-tuned count is not honoured inside a full-throttle scope.","title":"A full budget raises, it does not merely permit"},{"category":"section","location":"composition/#Worked-example","page":"Composition rules","text":"With Threads.threadpoolsize() == 8:\n\nloop trip count budget BLAS/FFTW inside Polyester/NFFT inside\n@budgeted_threads for i in 1:32 32 max(1, 8 ÷ 32) = 1 1 off\n@budgeted_threads for i in 1:2 2 8 ÷ 2 = 4 4 off\n@budgeted_threads for i in 1:1 1 8 ÷ 1 = 8 8 on\n@budgeted_threads threads = false for i in 1:2 — 1 1 off","title":"Worked example"},{"category":"section","location":"#NestedThreading.jl","page":"Home","text":"note: Disclaimer\nThis package was written mostly by Claude (Anthropic's coding assistant), under human direction and review. The design decisions, benchmark numbers and documentation here were produced that way; treat the code as you would any other third-party dependency and read it before relying on it.\n\nCoordinate one CPU thread budget across independently-threaded libraries, so an outer parallel loop does not oversubscribe the machine with inner threading.","title":"NestedThreading.jl"},{"category":"section","location":"#The-problem","page":"Home","text":"BLAS, FFTW, Polyester and NFFT each decide independently how many threads to use, and none of them knows it is being called from inside a Threads.@threads loop that already occupies every core:\n\nThreads.@threads for i in 1:32          # 8 workers\n    mul!(view(Y, :, i), A, view(X, :, i))   # ...each starting 8 BLAS threads\nend\n\nThat is 64 runnable threads on 8 cores. Python solves this with threadpoolctl; this package is the Julia equivalent.","title":"The problem"},{"category":"section","location":"#Usage","page":"Home","text":"using NestedThreading\n\n@budgeted_threads for i in 1:32\n    mul!(view(Y, :, i), A, view(X, :, i))\nend\n\nWith Threads.threadpoolsize() == 8 the loop of 32 items already saturates the machine, so every registered library is limited to one thread inside the body. A loop of 2 items instead gets a budget of 4, for 2 workers × 4 inner threads. The arithmetic is max(1, capacity() ÷ length(range)), evaluated at runtime — the range may have a length that is only known then.\n\nThe budget is applied around the loop, once, before the parallel region opens; it is not re-entered on every iteration.","title":"Usage"},{"category":"section","location":"#Turning-the-loop-off","page":"Home","text":"Many operators carry a threaded::Bool. Pass it straight through:\n\n@budgeted_threads threads = op.threaded for i in eachindex(op.batch_indices)\n    ...\nend\n\nthreads = false runs a plain sequential loop and restricts inner libraries to a single thread, on the assumption that a caller who switched threading off did so because concurrency is happening somewhere else.","title":"Turning the loop off"},{"category":"section","location":"#Polyester-loops","page":"Home","text":"@budgeted_batch is the same thing for Polyester.@batch. The calling module needs import Polyester, since the expansion emits Polyester.@batch.","title":"Polyester loops"},{"category":"section","location":"#Loops-that-are-not-for-loops","page":"Home","text":"@sync/@spawn blocks, or a single call gated on a flag, use the function forms:\n\nwith_restricted_threads() do\n    @sync for idx in indices\n        Threads.@spawn work(idx)\n    end\nend\n\nop.threaded ? with_full_threads(() -> f(x)) : with_restricted_threads(() -> f(x))\n\nwith_full_threads actively enables libraries that default to off (NFFT), and is still clamped by any outer restriction, so it is safe to call from inside a batch loop.","title":"Loops that are not for loops"},{"category":"section","location":"#Supported-libraries","page":"Home","text":"Registration is automatic via package extensions — have the package loaded and it works:\n\nLibrary Kind Control used\nBLAS counted BLAS.get_num_threads/set_num_threads via libblastrampoline\nMKL counted MKL.get_num_threads/set_num_threads, MKL's own native functions\nFFTW counted FFTW.get_num_threads/set_num_threads\nNFFT counted (boolean) NFFT._use_threads[]\nPolyester guarded disable_polyester_threads\n\nMKL gets its own pool alongside the always-on BLAS one: BLAS.set_num_threads is supposed to forward to MKL through libblastrampoline, but the two do not reliably stay in sync (MKL.jl#174), so a budget scope also drives MKL's native thread count directly.\n\nSee Adding a library to register one this package does not ship.","title":"Supported libraries"}]
}
